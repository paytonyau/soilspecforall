% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling.R
\name{split_clhs}
\alias{split_clhs}
\title{Split Data via cLHS}
\usage{
split_clhs(spectra, split_ratio = 0.7, pc_selection = 0.999)
}
\arguments{
\item{spectra}{A numeric matrix where rows are samples and columns are wavelengths.}

\item{split_ratio}{A numeric value for the proportion of data in the calibration set. Defaults to 0.7.}

\item{pc_selection}{A numeric value for the cumulative variance to be explained by PCA. Defaults to 0.999.}
}
\value{
A list containing two integer vectors: `calibration_idx` and `validation_idx`.
}
\description{
This function splits a spectral dataset into calibration and validation sets
using the Conditioned Latin Hypercube Sampling (cLHS) method on the
principal components of the data.
}
\examples{
\dontrun{
# Create a dummy matrix
spec_matrix <- matrix(rnorm(500), nrow = 50, ncol = 10)
colnames(spec_matrix) <- 1:10
# Get split indices
splits <- split_clhs(spec_matrix)
# Create calibration and validation sets
cal_set <- spec_matrix[splits$calibration_idx, ]
val_set <- spec_matrix[splits$validation_idx, ]
}
}
